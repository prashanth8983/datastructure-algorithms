#include <bits/stdc++.h>
using namespace std;

int main()
{
    string s1, s2;
    getline(cin, s1);
    getline(cin, s2);
    string shuffle;
    getline(cin, shuffle);
    
    int l = shuffle.length();
    int i=0,j=0,k=0;
    while (i < l){
        if(shuffle[i] == )
    }
}