#include <bits/stdc++.h>

using namespace std;

void merge_sort(vector<int> &A, int l,int m,int r)
{
    if(i < j)
    {
        int mid = 
        merge_sort
    }
}

void merge(vector<int> &A, int l,int m,int r)
{
    if()
}

int main()
{
    int n;
    cin>>n;
    vector<int> A(n);
    for(auto &x: A)
        cin>>x;
    return 0;
}